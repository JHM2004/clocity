const x = 1;
const y = 2;


// A single-line comment
function greet(name) {
    /* block comment */
    let url = "http://example.com";
    let msg = `hello ${name}`;  // mixed
    return msg;
}
// Another comment
console.log(greet("world")); /* mixed two */
let result = x + y;
